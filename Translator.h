#include "Model.h"

class Translator
{
  public:
    Translator();
    ~Translator();
    string translateEnglishWord(string word);
    string translateEnglishSentence(string sentence);
    string translateTutWord(string tutWord);
    string translateTutSentence(string tutSentence);
  private:
};
